# 自定义日志格式配置文件
# 用于定义非标准日志格式的解析规则
#
# 格式说明:
# name:     格式名称 (必填)
# regex:    正则表达式，使用捕获组定义字段 (必填)
# field:    字段名，按顺序对应每个捕获组 (必填，至少一个)

name: application-json-structured
regex: ^\[([^\]]+)\] \[([^\]]+)\] \[([^\]]+)\] (.*)$
field: timestamp
field: level
field: component
field: message
